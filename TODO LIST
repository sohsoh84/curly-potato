remove all staging area backups after commit? (should you?)
git add --redo
alias

backup and undo backups in reset for redo
check if the git add fucking works with deleted files and if not, what the fuck you should do, it fucking dosnt work with status
check stagings using lstversion in git add -n

LOG -since and -before with better time formats

number of files commited(log), add the removed files to this

cupot add .

checking if there is no uncommited files in checkout dosnt work with deleted files // probably done

status --all dosnt work

clear empty Directories

git status with T

implement hard revert